import ChangePassword from '@/components/Settings/ChangePassword';
import MainContainer from '@/components/Settings/MainContainer';

export default function ProfileSettingPassword() {
  return (
    <MainContainer>
      <ChangePassword />
    </MainContainer>
  );
}
